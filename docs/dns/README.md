# DNS records for digits.dpdns.org

Generated by `npm run dns` from the live Resend configuration. Regenerate rather
than editing — the DKIM key changes if the domain is re-added in Resend.

**Fastest path:** Cloudflare → DNS → Records → **Import and Export → Import** →
upload `digits.dpdns.org.zone`. Every name in it is fully qualified, so nothing depends on
which zone it lands in.

| Purpose | Type | Name | Value | Priority |
| --- | --- | --- | --- | --- |
| GitHub Pages | CNAME | `digits.dpdns.org` | `yakyakyak14.github.io.` | — |
| Resend DKIM | TXT | `resend._domainkey.digits.dpdns.org` | `p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC7zQI2eVFEvtvOmes…` | — |
| Resend SPF | MX | `send.digits.dpdns.org` | `feedback-smtp.us-east-1.amazonses.com` | 10 |
| Resend SPF | TXT | `send.digits.dpdns.org` | `v=spf1 include:amazonses.com ~all` | — |
| Cloudflare Email Routing | MX | `digits.dpdns.org` | `route1.mx.cloudflare.net.` | 2 |
| Cloudflare Email Routing | MX | `digits.dpdns.org` | `route2.mx.cloudflare.net.` | 21 |
| Cloudflare Email Routing | MX | `digits.dpdns.org` | `route3.mx.cloudflare.net.` | 31 |
| Cloudflare Email Routing | TXT | `digits.dpdns.org` | `v=spf1 include:_spf.mx.cloudflare.net include:amazonses.c…` | — |
| DMARC | TXT | `_dmarc.digits.dpdns.org` | `v=DMARC1; p=none;` | — |

## Three things that will silently break this

**1. Resend's record names are relative to `dpdns.org`, not to `digits.dpdns.org`.**
The dashboard shows `send.digits` and `resend._domainkey.digits`. Typed into a
`digits.dpdns.org` zone as-is, those become `send.digits.digits.dpdns.org`. Nothing errors — the
domain simply stays at *not_started* forever. The generated records above are
already absolute, which removes the question.

**2. You get one set of MX records, so pick one inbound provider.**
The table uses **Cloudflare Email Routing**, which forwards `info@digits.dpdns.org` to
`yakyakyak1414@gmail.com` with no code. Resend's *Enable Receiving* wants its own MX
(`inbound-smtp…amazonses.com`) and delivers over a webhook rather than to a
mailbox — it cannot forward to Gmail on its own. **Turn Resend's receiving toggle
off** so the two do not compete.

The SPF record above already includes both `_spf.mx.cloudflare.net` and
`amazonses.com`, so receiving through Cloudflare and sending through Resend
coexist correctly.

**3. An apex CNAME alongside MX is only legal because of Cloudflare.**
`digits.dpdns.org` carries both a CNAME (to GitHub Pages) and MX records. Standard DNS
forbids that. Cloudflare's CNAME flattening makes it work by answering with A
records. On a DNS host without flattening, swap the CNAME for GitHub's four A
records.

## After importing

1. Cloudflare → **Email → Email Routing → Enable**, then create the address
   `info@digits.dpdns.org` → `yakyakyak1414@gmail.com` and click the confirmation link Cloudflare
   sends to that inbox. The route stays inactive until you do.
2. GitHub repo → **Settings → Pages → Custom domain** → `digits.dpdns.org`, then enable
   **Enforce HTTPS** once the certificate is issued.
3. Wait for Resend to report **Verified**, then switch the sender:

   ```bash
   # .env
   EMAIL_FROM="DIGITs Election Watch <info@digits.dpdns.org>"
   npm run fn:deploy -- --secrets
   ```

   Do not switch it before Resend verifies — an unverified sender returns 403 and
   no DIGEO email goes out at all.

## Verify

```bash
curl -I https://digits.dpdns.org                      # 200
nslookup -type=MX digits.dpdns.org                    # three Cloudflare MX
nslookup -type=TXT resend._domainkey.digits.dpdns.org # the DKIM key
npm run dns:check                            # re-reads Resend's status
```
